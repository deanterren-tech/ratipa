# Аудит модуля «План Дохода» (PlanDohodModule.tsx + planDohodService.ts)

Дата: 04.09.2026
Файлы:
- `/src/components/modules/PlanDohodModule.tsx` — 3507 строк
- `/src/firebase/planDohodService.ts` — 380 строк
- `/src/types.ts` — TripPlan, LegPlan, PotentialLoad
- `/src/utils/financeCalculators.ts` — calculateTripFinances

---

## 1. ОШИБКИ (TypeScript / Логика)

### 1.1 Критические логические ошибки

| # | Строка | Проблема | Серьёзность |
|---|--------|----------|-------------|
| 1 | 112-113 | `setDispatchers(withMe)` и `setDispatchersOrder(withMe)` — **обе переменные получают одинаковое значение**. `setDispatchers` должна получать упорядоченный список, а `setDispatchersOrder` — отдельно хранить порядок отображения. Сейчас порядок всегда равен списку, что ломает drag-and-drop перетасовку диспетчеров. | 🔴 Высокая |
| 2 | 1601 | `dispatcher: dispatcher || carDispatcherMapping[trimmedCar] || user.name` — `carDispatcherMapping` может не содержать ключа `trimmedCar` (новое авто без маппинга), тогда диспетчером назначается `user.name` вместо того, чтобы запросить у пользователя. Тихий баг. | 🟡 Средняя |
| 3 | 45 | `const [searchQuery, setSearchQuery] = useState("")` — **состояние объявлено, но нигде не используется**. Поиск использует `searchCarQuery` (строка 1002). | 🟡 Средняя |
| 4 | 1290-1303 + 1678-1689 | `calculateTotals()` вызывается на **каждом рендере** (не обёрнута в `useMemo`). Все поля `fin.days`, `fin.totalKm` и т.д. пересчитываются при любом изменении состояния, а не только при изменении legs/дат. | 🟡 Средняя |
| 5 | 1393-1458 | `parseAiRouteClient()` — определена, но **нигде не вызывается**. Мёртвый код. | 🟡 Средняя |
| 6 | 1279-1287 | `getTripDays()` — определена, но не вызывается. `calculateTripFinances` уже считает дни. | 🟡 Средняя |
| 7 | 1699-1700 | Двойное округление: `calculateTotals()` возвращает `fin.profitPerDay` (уже число), затем `Math.round(rawProfitPerDay)` — ещё раз. | 🟡 Средняя |
| 8 | 3044-3047, 3165-3168 | `(trip as any).updatedBy` и `.updatedAt` — поля не описаны в интерфейсе `TripPlan`, хотя Firebase их сохраняет через сервис. | 🟡 Средняя |
| 9 | 1155-1194 | `applyMapRoute` обёрнута в `useCallback`, но dependенсы включают `updateLeg` — обычную функцию, которая пересоздаётся на каждом рендере. `useCallback` бесполезен. | 🟡 Средняя |
| 10 | 1076-1095 | `getDispatcherColor()` определена, но **не используется** в JSX этого модуля (нужна для `TripList`, но `TripList` тоже не используется). | 🟡 Средняя |
| 11 | 67-68: сервис | `createTrip` использует `(trip as any).createdAt` и `(trip as any).createdBy` — обход типизации. | 🟡 Средняя |

### 1.2 TypeScript (типизация)

| # | Строка | Проблема |
|---|--------|----------|
| 12 | 107 | `const [distances, setDistances] = useState<any[]>([])` — должен быть `DistancePreset[]` |
| 13 | 117 | `const [currencies, setCurrencies] = useState<any[]>([])` — должен быть `CurrencyPreset[]` |
| 14 | 118 | `const [logs, setLogs] = useState<any[]>([])` — должен быть `AuditLog[]` |
| 15 | 108 | `const [settings, setSettings] = useState<any>(...)` — нет интерфейса для настроек |
| 16 | 1196-1201 | `handleUpdateLegRoute` параметр `updatedFields: any` |
| 17 | 839-861 | `document.querySelectorAll(".car-strip-item")` — нет гарантии наличия элементов, TS не проверяет |
| 18 | 3214 | `style={{ zoom: tableScale / 100 } as any}` — `zoom` нестандартное CSS-свойство |

**TypeScript компилятор:** На `PlanDohodModule.tsx` — 0 ошибок (все ошибки в других файлах проекта). `any`-маскировка — основная проблема.

---

## 2. ОПТИМИЗАЦИЯ

### 2.1 Неиспользуемый код / импорты

| # | Сущность | Тип |
|---|----------|-----|
| 1 | `TripList` (импорт строка 4) | Мёртвый импорт — компонент нигде не рендерится, хотя его пропсы (`getDispatcherColor`, `handleTripDrop`, `finishTripToArchive`) частично задействованы в `renderTripsGrid` |
| 2 | `Check`, `Bot`, `Receipt` из `lucide-react` (строка 31-37) | Импортированы, но не используются в JSX |
| 3 | `parseAiRouteClient` (строка 1393) | Мёртвая функция (готовый парсер AI-маршрутов) |
| 4 | `getTripDays` (строка 1279) | Мёртвая функция (дублирует логику `calculateTripFinances`) |
| 5 | `getDispatcherColor` (строка 1076) | Определена, не используется (была нужна TripList) |
| 6 | `searchQuery` / `setSearchQuery` (строка 45) | Мёртвое состояние (есть `searchCarQuery`) |
| 7 | `renderNotebookWidget` bookend эффекты (строка 196-198) | При открытии блокнота `toggleNotebook` сбрасывает `isNbMinimized` и корректирует координаты — это **логика изменения состояния в сеттере** (side effect в `setIsNotebookOpen`). |

### 2.2 Дублирование

| # | Описание |
|---|----------|
| 1 | **Сортировка** — `activeTripsComputed` (2675-2740) и `archiveTripsComputed` (2752-2799) содержат почти идентичный блок сортировки (~40 строк каждый). Разница: `archiveTripsComputed` не сортирует по `profitDay` и не поддерживает `manualTripsOrder`. |
| 2 | **Метрики карточки рейса** — одни и те же `profit`, `freight`, `km`, `expenses`, `days`, `perDay` вычисляются трижды: для десктопа (3088-3117), для мобильного (3174-3199) и в инициале строки (2985-3117). |
| 3 | **Таблица vs карточки** — для Legs (десктоп: 1839-1973, мобильные: 1976-2129) и для PotentialLoads (десктоп: 2352-2475, мобильные: 2478-2536). ~300 строк на layout-дублирование на секцию. |
| 4 | **`calculateTotals()` и `calculatePlTotals()`** — обе вызывают `calculateTripFinances`, отличаются только аргументами. |
| 5 | **Логика addLeg/removeLeg дублирована** для `legs` и `plLegs` — две независимые реализации с одинаковой структурой (splice). |

### 2.3 Производительность

| # | Проблема | Влияние |
|---|----------|---------|
| 1 | **`handleNoteChange` → `pdService.saveNotebookNote` на каждый нажатый символ** (строка 515-517). Каждый символ в заметке блокнота вызывает `set()` в Firebase RTDB. При быстром наборе — десятки запросов/сек. | 🔴 Высокая — расход трафика, лимиты Firebase |
| 2 | **Нет виртуализации списка** (строка 2964-3202). Комментарий: Virtuoso отключён из-за конфликта с CSS zoom. При 200+ рейсах DOM → тысячи элементов. | 🟡 Средняя |
| 3 | **`cars.filter()` трижды** (строка 609-611) — для `countBaza`, `countReis`, `countNone`. Один проход был бы в 3x быстрее. | 🟢 Низкая |
| 4 | **`localStorage.getItem("ratipa_nb_height_${user.name}")`** читается при рендере каждого textarea (строй 911-912). | 🟢 Низкая |
| 5 | **`calculateTotals()` вызывается на каждом рендере** (строка 1678-1689 destructure). Не обёрнута в `useMemo`. | 🟡 Средняя |
| 6 | **`updateLeg` пересоздаёт массив `setLegs(prev => prev.map(...))`** на каждый вызов даже если не было изменений (строка 1226-1265). | 🟢 Низкая |
| 7 | **Состояние блокнота (высота, координаты, видимость)** — каждый чекбокс/textarea рендера сохраняет в localStorage при каждом рендере. | 🟢 Низкая |

### 2.4 Размер модуля

- **3507 строк** — нарушает принцип единой ответственности. Один файл содержит:
  - Форму создания/редактирования (модалка)
  - Таблицу рейсов (активные + архив)
  - Блокнот (floating widget)
  - Историю изменений
  - AI-парсер маршрутов
  - Карту маршрутов (MapRouteModal)
  - Управление масштабом
  - Подсветку рейсов
  - Drag-and-drop перетасовку

**Рекомендация:** Выделить в отдельные компоненты:
- `NotebookWidget.tsx` (~400 строк, стр. 598-998)
- `TripFormModal.tsx` (~950 строк, стр. 1691-2645)
- `TripCard.tsx` (~120 строк каждая карточка)
- `KpiSummaryPanel.tsx` (~50 строк)
- Potential Loads sub-forms

### 2.5 Безопасность

| # | Проблема | Описание |
|---|----------|----------|
| 1 | **Нет проверки `user.permissions.planDohod`** — интерфейс `UserPermissions` содержит `planDohod: "none" | "read" | "write"`, но компонент не проверяет его нигде. Любой пользователь, открывший модуль, может создавать/редактировать/удалять рейсы. | 🟡 Средняя |
| 2 | **`dbService.saveVehicle()` без проверки** (строка 1573) — `saveTrip` сохраняет авто в справочник `vehicles`. Любой пользователь может замусорить справочник. | 🟡 Средняя |
| 3 | **`useFirebase` guard в сервисе** — при `useFirebase=false` данные пишутся в localStorage. Методы `getLocalData` и `setLocalData` не имеют ограничений по размеру. | 🟢 Низкая |

---

## 3. AI-ВОЗМОЖНОСТИ

### 3.1 Готовый, но неиспользуемый AI-код

| Компонент | Где | Статус |
|-----------|-----|--------|
| `parseAiRouteClient()` | строка 1393 | ✅ Полностью готовая функция для парсинга AI-сгенерированного маршрута из текста. Поддерживает русские/английские города, ставки, паромы, коэфф. **Не вызывается.** |
| `Bot` из `lucide-react` | импорт стр. 35 | Импортирован для UI, но не используется |

### 3.2 Предложения по AI-интеграции

**A. AI-заполнение маршрута (рекомендуется — минимальные усилия)**
- Использовать `parseAiRouteClient` для вставки маршрута из буфера обмена
- Добавить кнопку «🧠 Вставить маршрут» в заголовок плечей (строка 1836)
- При нажатии — prompt для ввода текста маршрута → парсинг → заполнение legs
- Уже есть готовый парсер, `Bot` иконка, бизнес-логика

**B. AI-рекомендации по маршруту**
- На основе `calculateEuroFreight` и NBRB курсов — AI может предложить оптимальный маршрут
- Анализ исторических данных через `logs` (история изменений)

**C. AI-суммаризация**
- AI может генерировать `tripNote` на основе legs, direction, dispatcher
- AI может анализировать общую KPI-сводку

**D. AI-предиктивная аналитика**
- Прогноз прибыли на основе исторических `profitFact` из архива
- Оценка маржинальности до создания рейса

**E. AI-проверка данных**
- Валидация корректности введённого маршрута (нелогичные точки, пропущенные сегменты)
- Проверка ставок на аномалии

### 3.3 Быстрые победы (реализация < 1 часа)

1. **Интегрировать `parseAiRouteClient`** — добавить кнопку в модалку плечей, prompt→parse→fill
2. **Убрать мёртвый код** — `Check`, `Bot`, `Receipt` из импортов
3. **Включить TripList** — заменить `renderTripsGrid` на `TripList` (если он функционально совпадает), избавившись от 200+ строк дублирования
4. **Добавить `useMemo` для `calculateTotals()`** — обернуть в useMemo с зависимостями `[legs, dateStart, dateEnd, extraExpense, ferryCost, factKm]`
5. **Debounce `handleNoteChange`** — задержка 300-500ms перед Firebase write

---

## Резюме

| Категория | Найдено | Критических | Средних | Низких |
|-----------|---------|-------------|---------|--------|
| Ошибки (логика) | 11 | 1 | 9 | — |
| Ошибки (типизация) | 7 | — | 5 | 2 |
| Мёртвый код | 7 | — | 3 | 4 |
| Дублирование | 5 | — | 4 | 1 |
| Производительность | 7 | 1 | 3 | 3 |
| Безопасность | 3 | — | 2 | 1 |
| AI-возможности | 5 предложений | — | — | — |

**Рекомендуемые действия:**
1. 🔴 Исправить `setDispatchers/setDispatchersOrder` — отделить список от порядка
2. 🔴 Debounce `handleNoteChange` — перестать писать в Firebase на каждый символ
3. 🟡 Удалить мёртвый код (`parseAiRouteClient` — оставить, но вызвать; `searchQuery`, `getTripDays`, лишние импорты)
4. 🟡 Обёрнуть `calculateTotals()` в `useMemo`
5. 🟡 Выделить `renderNotebookWidget` в отдельный файл
6. 🟡 Добавить проверку `user.permissions.planDohod` перед записью
7. 🟡 Интегрировать AI-парсинг маршрута (готов на 90%)